export default {

    /**
     |--------------------------------------------------------------------------
     | Application origin
     |--------------------------------------------------------------------------
     |
     | This value is the origin of your application. This value is used when the
     | framework needs to place the application's origin in
     | any other location as required by the application or its packages.
     |
     */
    origin: ['http://127.0.0.1:3000'],

    /**
     |--------------------------------------------------------------------------
     | Application Methods
     |--------------------------------------------------------------------------
     |
     | This value is the Application Methods of your application. This value is used when the
     | framework needs to place the application's Application Methods in
     | any other location as required by the application or its packages.
     |
     */
    methods: ['GET', 'POST', 'PUT', 'DELETE', 'HEAD', 'OPTIONS', 'PATCH'],

    /**
     |--------------------------------------------------------------------------
     | Application allowed Headers
     |--------------------------------------------------------------------------
     |
     | This value is the allowed Headers request of your application. This value is used when the
     | framework needs to place the application's allowed Headers in
     | any other location as required by the application or its packages.
     |
     */
    allowedHeaders: ['Content-Type']

}